# Vendored Browser Assets

Phase 1J requires no third-party browser bundle, so this directory intentionally contains no library files.

Future additions must be explicitly versioned, include license/source metadata here, and be committed locally. Production pages must not load browser assets from a public CDN.
